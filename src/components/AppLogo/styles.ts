import styled from 'styled-components';

export const LogoContainer = styled.span`
	display: inline-flex;
	align-items: center;
	gap: 0.5em;

	svg {
		fill: currentColor;
		stroke-linejoin: miter;
		stroke-linecap: butt;
	}
`;
